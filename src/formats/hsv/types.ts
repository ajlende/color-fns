/**
 * Object representation of hue, saturation, value, and alpha.
 *
 * @example
 * ```
 * const hsv: Hsv = {
 *   h: 210,
 *   s: 0.5,
 *   v: 0.6,
 *   a: 0.8,
 * }
 * ```
 */
export interface Hsv {
	h: number
	s: number
	v: number
	a: number
}
