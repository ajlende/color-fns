/**
 * RGB CSS string as used in CSS.
 *
 * @example
 * ```
 * const legacyRgb: RgbCss = "rgb(255, 0, 0)"
 * const modernRgb: RgbCss = "rgb(255 0 0)"
 * const legacyRgba: RgbCss = "rgba(255, 0, 0, 0.5)"
 * const modernRgba: RgbCss = "rgba(255 0 0 / 0.5)"
 * ```
 */
export type RgbCss = string
