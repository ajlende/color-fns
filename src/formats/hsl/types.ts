/**
 * Object representation of hue, saturation, lightness, and alpha.
 *
 * @example
 * ```
 * const hsl: Hsl = {
 *   h: 210,
 *   s: 0.5,
 *   l: 0.4,
 *   a: 0.8,
 * }
 * ```
 */
export interface Hsl {
	h: number
	s: number
	l: number
	a: number
}
