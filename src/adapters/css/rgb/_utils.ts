/**
 * RGB CSS string as used in CSS.
 *
 * @example
 * ```
 * const legacyRgb: CssRgb = "rgb(255, 0, 0)"
 * const modernRgb: CssRgb = "rgb(255 0 0)"
 * const legacyRgba: CssRgb = "rgba(255, 0, 0, 0.5)"
 * const modernRgba: CssRgb = "rgba(255 0 0 / 0.5)"
 * ```
 */
export type RgbCss = string
